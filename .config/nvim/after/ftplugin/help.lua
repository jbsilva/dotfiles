-------------------------------------------------------------------------------
--> Help
--
-- Open help to the right instead of in a horizontal split above. Help is
-- written to 78 columns, so a vertical split fits it exactly and leaves the
-- code readable next to it; this matches 'splitright' in options.lua.
-------------------------------------------------------------------------------
vim.cmd.wincmd('L')
